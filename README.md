# che-server-standalone-test
Test environment for Eclipse Che server PRs in standalone mode. Includes scripts and configs to run che-server with Podman, Gitea (GitHub-compatible proxy), and CRC. No full Eclipse Che deployment needed.
